apiVersion: v1
kind: Service
metadata:
  name: cart
  namespace: flipkart
  labels:
    project: flipkart
    tier: backend
    component: cart
spec:
  selector:
    project: flipkart
    tier: backend
    component: cart
  ports:
    - protocol: TCP
      port: 8080 # service port
      targetPort: 8080 # container por