apiVersion: v1
kind: Service
metadata: 
  name: user
  namespace: flipkart
  labels:
    project: flipkart
    tier: backend
    component: user
spec:
  selector:
    project: flipkart
    tier: backend
    component: user
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080